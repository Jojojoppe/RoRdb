## NEST RoR database backend
Create file `dburl.php` in the cfg folder with the database query URL as in
```php
<?php
	$database_url = "https://docs.google.com/spreadsheets/d/[spreadsheet ID]/gviz/tq?tq=";
?>
```
